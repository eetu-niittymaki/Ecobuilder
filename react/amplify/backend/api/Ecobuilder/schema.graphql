type Organisationalsolution @model @auth(rules: [{allow: public}]) {
  id: ID!
  positionId: Int
  Title: String
  Content: String
  Popup: String
}

type Planningsolution @model @auth(rules: [{allow: public}]) {
  id: ID!
  positionId: Int
  Title: String
  Content: String
  Popup: String
}

type Technicalsolution @model @auth(rules: [{allow: public}]) {
  id: ID!
  positionId: Int
  Title: String
  Content: String
  Popup: String
}
 